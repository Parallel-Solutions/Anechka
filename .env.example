# BITRIX_WEBHOOK_URL=   # необязательно; обычно задаётся на странице Настройки
# OPENAI_API_KEY=       # необязательно; ключ OpenAI или VseLLM, можно задать на странице Настройки
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-4o
OPENAI_BITRIX_METADATA_MODEL=
# Для VseLLM используйте:
# OPENAI_BASE_URL=https://api.vsellm.ru/v1
# OPENAI_MODEL=openai/gpt-5
APP_SECRET_KEY=change-me

# Bootstrap admin (создаётся при первом старте, если пользователя ещё нет)
BOOTSTRAP_ADMIN_EMAIL=admin@example.com
BOOTSTRAP_ADMIN_PASSWORD=change-me
COOKIE_SECURE=false

# Тесты: APP_AUTH_DISABLED=1 отключает обязательный вход
# APP_AUTH_DISABLED=1

# --- Docker Compose: prod vs local dev ---
# Prod (Linux-сервер): НЕ задавайте COMPOSE_FILE — порт 80 по умолчанию.
# WEB_PUBLISH_PORT=80
#
# Локальная разработка (Windows): раскомментируйте все три строки
# или выполните: .\scripts\setup-local-env.ps1
# COMPOSE_PATH_SEPARATOR=:
# COMPOSE_FILE=docker-compose.yml:docker-compose.dev.yml
# WEB_PUBLISH_PORT=8000

# PostgreSQL
POSTGRES_USER=bitrix
POSTGRES_PASSWORD=bitrix
POSTGRES_DB=bitrix_export
# Внутри docker-сети БД доступна как db:5432 (compose переопределяет DATABASE_URL).
# Для доступа с хоста используйте dev-override: docker compose -f docker-compose.yml -f docker-compose.dev.yml up
# (публикует PostgreSQL на localhost:5433).
DATABASE_URL=postgresql+psycopg://bitrix:bitrix@db:5432/bitrix_export

# Тесты: docker compose exec web pytest
# Для тестов миграций на реальном Postgres (опционально):
# TEST_DATABASE_URL=postgresql+psycopg://bitrix:change-me@db:5432/bitrix_export_test

EXPORT_DIR=./exports
FILE_STORAGE_DIR=./filestorage
LOG_LEVEL=INFO

# Bitrix import worker
WORKER_POLL_INTERVAL=2.0
WORKER_HEARTBEAT_INTERVAL=30.0
WORKER_STALE_RUN_MINUTES=15
IMPORT_BATCH_SIZE=50
IMPORT_OVERLAP_MINUTES=10
BITRIX_METADATA_PROMPT_VERSION=1
BITRIX_IMPORT_SCHEDULE_ENABLED=true
BITRIX_IMPORT_SCHEDULE_INTERVAL_MINUTES=60

# Call result import / LLM classification
# Fallback Bitrix user when deal has no assignee (tasks.task.add RESPONSIBLE_ID)
BITRIX_SERVICE_USER_ID=464
CALL_RESULTS_BITRIX_EXECUTION_ENABLED=false
BITRIX_CALL_SOURCE_FIELD_CODE=
BITRIX_CALL_SOURCE_FIELD_VALUE=
POSITIVE_ACTIVITY_DEFAULT_DEADLINE=24h
CALL_RESULTS_TIMEZONE=Europe/Moscow
TOMORU_DEFAULT_LOCAL_CALL_TIME=10:00
# Current Tomoru REST API. Creating a pending batch and launching real calls
# are controlled by separate flags.
TOMORU_API_BASE_URL=https://app.tomoru.ru
TOMORU_API_KEY=
TOMORU_AGENT_ID=
TOMORU_BATCH_CREATION_ENABLED=false
TOMORU_BATCH_AUTO_LAUNCH_ENABLED=false
TOMORU_BATCH_MAX_RETRIES=3
TOMORU_BATCH_RETRY_DELAY_SECONDS=300
TOMORU_RESULT_CALLBACK_URL=
# Tomoru event integration is disabled by default. Keep false until the
# microservice is connected to a test agent and one safe test call is approved.
TOMORU_EVENTS_ENABLED=false
TOMORU_BOT_ID=
TOMORU_EVENT_NAME=coldCall
TOMORU_PUBLIC_BASE_URL=
TOMORU_WEBHOOK_SECRET=
TOMORU_CALLBACK_ALLOWED_HOSTS=europe-west1-tomoru-2bb77.cloudfunctions.net,api.tomoru.ru
TOMORU_HTTP_TIMEOUT_SECONDS=10
CALL_RESULTS_MAX_EXECUTION_RETRIES=3
CONTACT_SEARCH_PROVIDER=fake
# Для ручной проверки «Найти другой контакт» в prod: CONTACT_SEARCH_PROVIDER=lpr
CONTACT_SEARCH_AUTO_CONFIRM_THRESHOLD=0.95
LLM_CALL_RESULTS_ENABLED=true
LLM_CALL_RESULTS_USE_MOCK=false
LLM_CALL_RESULTS_CONFIDENCE_THRESHOLD=0.80
LLM_CALL_RESULTS_CONCURRENCY=3
LLM_CALL_RESULTS_CACHE_ENABLED=true
# LLM_CALL_RESULTS_MODEL=gpt-4o-mini
CALL_RESULT_MAX_FILE_BYTES=52428800
CALL_RESULT_MAX_ROWS=100000
